import DeckClient from "./deck-client";

/** /presentacion — el deck del Programa +1, UNO solo para México y
 * Estados Unidos (decisión de Manuel, 5-ago-2026): montos en las dos
 * monedas con referencia EE. UU., y cada evento enseña su país. */

export default function PaginaPresentacion() {
  return <DeckClient />;
}
